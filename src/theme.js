import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#fc8caa',
            light: '#ffbedc',
            dark: '#c75c7b',
        },
        secondary: {
            main: '#ffecb9',
            light: '#ffffec',
            dark: '#cbba89',
        },
    },
});

export default theme;
